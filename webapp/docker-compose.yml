services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: puffin
      POSTGRES_PASSWORD: puffin
      POSTGRES_DB: puffin
    ports:
      - "5432:5432"
    volumes:
      - puffin_pgdata:/var/lib/postgresql/data

volumes:
  puffin_pgdata:
